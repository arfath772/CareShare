# Server Configuration
PORT=8080
NODE_ENV=development
APP_BASE_URL=http://localhost:8080

# Database Configuration
DB_NAME=zero_db
DB_USER=root
DB_PASSWORD=your_database_password_here
DB_HOST=localhost
DB_PORT=3306

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here_at_least_256_bits_long
JWT_EXPIRATION=86400000

# Email Configuration (Gmail SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_password_here
MAIL_FROM="CareNShare <your_email@gmail.com>"

# Password Reset Configuration
PASSWORD_RESET_EXPIRATION_MINUTES=15

# Instructions:
# 1. Copy this file to .env (don't commit .env to git)
# 2. Replace all placeholder values with your actual credentials
# 3. For Gmail, you MUST use an App Password (not your regular password)
#    - Go to: https://myaccount.google.com/apppasswords
#    - Generate a new app password for "Mail"
#    - Use that 16-character password in MAIL_PASSWORD
# 4. Make sure your database exists: CREATE DATABASE IF NOT EXISTS zero_db;
# 5. Run the migration script after setting up: node scripts/migrate-database.js
